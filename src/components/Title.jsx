import React from "react";

const Title = ({title}) => {
  return (
    <h1 className="pb-1 text-xs font-medium uppercase tracking-widest text-neutralC-800">
      {title}
    </h1>
  );
};

export default Title;
